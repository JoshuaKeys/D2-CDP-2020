import { NumberToTimePipePipe } from './number-to-time-pipe.pipe';

describe('NumberToTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
